# ============================================
# Sentinel — Logrotate konfiguratsiya
# Joylashuv: /etc/logrotate.d/sentinel
# ============================================

/var/log/sentinel/sentinel.log {
    weekly
    rotate 4
    compress
    delaycompress
    missingok
    notifempty
    create 0640 root root
}
